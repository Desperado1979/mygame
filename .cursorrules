# 《破晓纪元》项目开发准则 (Project EpochOfDawn Rules)

## 1. 核心架构规约
- 客户端环境：Unity 2022 LTS + URP。
- 权威后端原则：客户端仅负责表现。所有关键逻辑（伤害、爆率、掉落、消耗）必须经过服务端校验。
- 数据驱动：禁止硬编码数值。优先从 JSON 配置文件（Config）中读取。

## 2. 战斗与伤害计算流水线 (严禁违反)
当涉及伤害计算时，必须严格遵守以下顺序：
1. 基础攻击计算：[攻击者总攻] - [防御者总防]。
2. 基础伤害底限：如果 (1) < 1，则结果 = 1。
3. 动态修正：应用 [浮动倍率] -> [暴击倍率] -> [增伤比]。
4. 减伤墙过滤：(3) 的结果 * (1 - 减伤比)。
5. 严禁逻辑：禁止在已经应用了防御减法后，再次重复计算复杂的防御百分比公式，防止双重减伤。

## 3. 存档与同步规约 (D5 阶段)
- 每次涉及资产、等级、物品变动，必须通过 `persist_sync` 模块。
- 必须导出三件套：State(状态)、Audit(审计日志)、Request(请求包)。
- 异常处理：任何非法操作（如技能 CD 未到却触发）必须抛出 `SrvVal_IllegalOperation` 给服务端审计。

## 4. 节省额度指令
- 除非我明确要求，否则禁止重写整个类。
- UI 布局代码优先使用 Unity 内置组件，逻辑部分保持精简。
- 简单需求（如 UI 文案、字符串、小范围显示逻辑）优先小范围补丁式修改，避免借机大重构。

## 5.1 里程碑原则（服务端可信）
- 每个里程碑收尾/复盘时应对照：**「这一步离服务端可信更近一步了吗？」** 更近则关联审计、协议、校验或权威边界；未更近则明确标注（如纯手感/表现），避免误判。

## 5. 与 README 进度表对齐（补漏范围）
- **只补**：根目录 **`README.MD`** 里 **「当前开发进度」主表**、**D0 / D1 / D2 子任务表**、**P1-A 子任务表** 中 **状态为「已完成」** 且 **工程仍缺失** 的脚本、配置或文档（以 `EpochOfDawn/` 为真源）。
- **不补**：同一表中为 **`进行中` / `未开始`** 的条目，或仅出现在「今日开发日志」长文、附录里的设想——除非用户**明确改表状态**或**单独下指令**。