# EpochOfDawn Server Placeholder (D5 Kickoff)

This folder is a minimal scaffold for D5 preparation.

## Scope
- Auth/session placeholder
- Character state persistence placeholder
- Economy and combat validation event intake

## Next steps
1. Add a minimal API service (`/login`, `/state/load`, `/state/save`).
2. Validate audit events from client (`gold`, `inventory`, `damage_summary`).
3. Add migration/versioning for save schemas.
